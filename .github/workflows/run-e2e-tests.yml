name: Manual Run E2E Tests Workflow
on:
  workflow_dispatch:
    inputs:
      compose-repository-branch:
        description: "Compose repository branch to use"
        type: string
        default: "main"
      compose-repository-compose-folder:
        description: "Compose repository folder with compose"
        type: string
        default: "docker-compose/apihub-generic"
      postman-collections-run:
        description: "Flag for executing postman collections"
        type: boolean
        default: true
      postman-collections-list:
        description: "Collections to execute"
        type: string
        default: "./e2e/1_1_Smoke_Portal.postman_collection.json,./e2e/1_3_Linter_Portal.postman_collection.json,./e2e/2_1_Negative_Portal.postman_collection.json,./e2e/3_1_Stories_Bugs.postman_collection.json,./e2e/3_2_Stories_Bugs.postman_collection.json,./e2e/4_Access_control.postman_collection.json"
      playwright-tests-run:
        description: "Flag for executing playwright tests"
        type: boolean
        default: true
      playwright-test-args:
        description: "Playwright test command CLI arguments"
        type: string
        default: "--project=Portal --workers=8 --grep-invert=@flaky"
      apihub-backend-image-tag:
        description: "APIHUB Backend docker image tag"
        type: string
        default: "main"
      apihub-build-task-consumer-image-tag:
        description: "APIHUB Build Task Consumer docker image tag"
        type: string
        default: "main"
      apihub-ui-image-tag:
        description: "APIHUB UI docker image tag"
        type: string
        default: "main"
      apihub-api-linter-service-image-tag:
        description: "APIHUB API Linter Service docker image tag"
        type: string
        default: "main"
      apihub-agents-backend-image-tag:
        description: "Agents Backend docker image tag"
        type: string
        default: "main"

permissions:
  contents: read
  packages: read

jobs:
  call-docker-compose-run-and-test-ci-workflow:
    uses: ./.github/workflows/e2e-compose.yml
    with:
      source-layout: monorepo
      compose-repository-branch: ${{ github.event.inputs.compose-repository-branch || 'main' }}
      compose-repository-compose-folder: ${{ github.event.inputs.compose-repository-compose-folder || 'docker-compose/apihub-generic' }}
      postman-collections-run: ${{ github.event.inputs.postman-collections-run == null || fromJson(github.event.inputs.postman-collections-run) }}
      postman-collections-list: ${{ github.event.inputs.postman-collections-list || './e2e/1_1_Smoke_Portal.postman_collection.json,./e2e/1_3_Linter_Portal.postman_collection.json,./e2e/2_1_Negative_Portal.postman_collection.json,./e2e/3_1_Stories_Bugs.postman_collection.json,./e2e/3_2_Stories_Bugs.postman_collection.json,./e2e/4_Access_control.postman_collection.json' }}
      playwright-tests-run: ${{ github.event.inputs.playwright-tests-run == null || fromJson(github.event.inputs.playwright-tests-run) }}
      playwright-test-args: ${{ github.event.inputs.playwright-test-args || '--project=Portal --workers=8 --grep-invert=@flaky' }}
      apihub-backend-image-tag: ${{ github.event.inputs.apihub-backend-image-tag || 'main' }}
      apihub-build-task-consumer-image-tag: ${{ github.event.inputs.apihub-build-task-consumer-image-tag || 'main' }}
      apihub-ui-image-tag: ${{ github.event.inputs.apihub-ui-image-tag || 'main' }}
      apihub-api-linter-service-image-tag: ${{ github.event.inputs.apihub-api-linter-service-image-tag || 'main' }}
      apihub-agents-backend-image-tag: ${{ github.event.inputs.apihub-agents-backend-image-tag || 'main' }}
      image-refs-json: >-
        {"backend":"ghcr.io/${{ github.repository_owner }}/qubership-apihub-backend:${{ github.event.inputs.apihub-backend-image-tag || 'main' }}","ui":"ghcr.io/${{ github.repository_owner }}/qubership-apihub-ui:${{ github.event.inputs.apihub-ui-image-tag || 'main' }}","build_task_consumer":"ghcr.io/${{ github.repository_owner }}/qubership-apihub-build-task-consumer:${{ github.event.inputs.apihub-build-task-consumer-image-tag || 'main' }}","api_linter_service":"ghcr.io/${{ github.repository_owner }}/qubership-api-linter-service:${{ github.event.inputs.apihub-api-linter-service-image-tag || 'main' }}","agents_backend":"ghcr.io/${{ github.repository_owner }}/qubership-apihub-agents-backend:${{ github.event.inputs.apihub-agents-backend-image-tag || 'main' }}"}
    secrets:
      APIHUB_ACCESS_TOKEN: ${{ secrets.APIHUB_ACCESS_TOKEN }}
      APIHUB_ADMIN_EMAIL: ${{ secrets.APIHUB_ADMIN_EMAIL }}
      APIHUB_ADMIN_PASSWORD: ${{ secrets.APIHUB_ADMIN_PASSWORD }}
      JWT_PRIVATE_KEY: ${{ secrets.JWT_PRIVATE_KEY }}
